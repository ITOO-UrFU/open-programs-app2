export class Competence {
    id: string;
    title: string;
    number: number;

    constructor( id: string,
                 title: string,
                 number: number ) {
      this.id = id;
      this.title = title;
      this.number = number;
    };
}

