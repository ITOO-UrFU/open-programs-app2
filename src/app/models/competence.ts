export class Competence {
    public id: string;
    public title: string;
    public number: number;

    constructor( id: string,
                 title: string,
                 number: number ) {
      this.id = id;
      this.title = title;
      this.number = number;
    };
}

